# pcmi-surrogate-model
Workflow for building a surrogate regression model to study PCMI phenomena in PWR-type rods. The dataset contains the main thermomechanical variables characterizing the nuclear fuel evolution during steady-state operation and power ramps. The model architecture is built through the open-source XGBoost library framework.
