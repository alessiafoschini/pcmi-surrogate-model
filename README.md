# pcmi-surrogate-model
Workflow for building a surrogate model to predict the magnitude of cladding deformations in PWR-type rods. The dataset contains the main thermomechanical variables characterizing the nuclear rods evolution during steady-state operation and power ramps. The model architecture is built through the open-source XGBoost library framework.
